import React from "react";

const Descriptiont = () => {
    const Product = {
        Description:"These shoes are perfect for running and outdoor activities, providing excellent support and comfort with every step. Lightweight, breathable, and durable."
            };
 return <p>{Product.Description}</p>;
}
export default Descriptiont;
